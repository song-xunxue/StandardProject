# 通用忽略规则（编译产物 / IDE / OS / 密钥 / 模型大文件等）
# 见全局 ~/.gitignore_global（git config core.excludesfile）
# 本文件只放本仓库的专属规则

# 环境变量与密钥（绝不推送）
.env
.env.*
!.env.example

# 数据库文件
*.db
*.sqlite
*.sqlite3

# Qt / 音乐播放器运行时依赖 DLL 与资源（体积大，不进库）
*.dll
