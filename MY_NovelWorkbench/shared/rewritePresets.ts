/**
 * 内置改写预设（v2-F13）：种子写入全局资源库（标记文件驱动，与内置结构模板同模式）
 * 载荷 instruction 为完整替换式指令——选中预设时替换「改写选中」的默认指令
 *
 * 作者: 李文煜
 * 日期: 2026-09-17
 */

import type { ResourceTemplate } from './types'

/** 去 AI 味改写指令（网文发布场景：平台拒 AI 稿的典型判定特征逐条针对性消除） */
const DEAI_INSTRUCTION = `请改写下面选中的文字，去除 AI 写作痕迹，保持情节事实与人称、时态不变，直接输出改写后的正文，不要任何说明。具体要求：
1. 删掉空洞的排比句式与「不是…而是…」类万能句，用具体动作、细节替代抽象概括；
2. 情绪不直接点破（如「他感到无比愤怒」），改用神态、动作、对话等可观察的行为呈现；
3. 打散整齐划一的句长节奏，长短句交错，避免连续段落以同构句式收尾；
4. 删减「仿佛」「似乎」「宛如」等高频喻词与陈旧比喻，保留必要的白描即可；
5. 对话去掉书面腔，口语化，允许省略、停顿与不完整句；
6. 删掉无信息量的过渡句、总结句与「空气中弥漫着…」类万能环境描写。`

/** 内置改写预设清单（listResources 首次调用种子写入；用户可删可改，删光不复活） */
export const BUILTIN_REWRITE_PRESETS: ResourceTemplate[] = [
  { kind: 'rewritePreset', name: '去AI味', payload: { instruction: DEAI_INSTRUCTION } }
]
